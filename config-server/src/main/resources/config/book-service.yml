spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db
    username: admin
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    producer:
      bootstrap-servers: 127.0.0.1:9092

server:
  port: 8082

application:
  open-api:
    email: elyashevich.artem06@gmail.com